import React from 'react';
import { SafeAreaView } from 'react-native';
import { LoginScreen } from './screens';

const App = () => {
  return (
    <SafeAreaView>
      <LoginScreen/>
    </SafeAreaView>
  );
};

export default App;
