import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  camera: {
    flex: 1,
  },
  container: {
    flex: 1,
    alignItems: 'center',
  },
  text: {
    position: 'absolute',
    bottom: 60,
    fontSize: 30,
    fontWeight: 'bold',
  },
});
